name: 🌅 아침 브리핑 자동 발송

on:
  # ── 매일 KST 06:00 = UTC 21:00 (전날) ──────────────────
  schedule:
    - cron: "3 22 * * *"


jobs:

      # ── 3. 의존성 설치 ─────────────────────────────────
      - name: 📦 패키지 설치
        run: |
          ls dir
          echo "hello"
          
